<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mycompany.overtest"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk 
        android:minSdkVersion="11"
        android:targetSdkVersion="11" />

	<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.GET_TASKS" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light">
        <activity
            android:label="@string/app_name"
            android:name="com.harad.bugnet.MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
			android:label="@string/app_name"
			android:name="com.harad.bugnet.Draw" >
        </activity>

		<activity
			android:label="@string/app_name"
			android:name="com.harad.bugnet.ListEditor" >
        </activity>
		
		<activity
			android:label="@string/app_name"
			android:name="com.harad.bugnet.Show" >
        </activity>
		
		<activity
			android:label="@string/app_name"
			android:name=".System" >
        </activity>
		
		<activity
			android:label="@string/app_name"
			android:name=".Settings" >
        </activity>
    
        <!--activity
			android:label="@string/app_name"
			android:name=".Step" >
        </activity-->
		
		<service
            android:name="com.harad.bugnet.GlobalTouchService" >
        </service>
	</application>

</manifest>
